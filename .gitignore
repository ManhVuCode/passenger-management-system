node_modules/
dist/
.env
.env.local
.env*.local
*.env
.DS_Store
coverage/
.turbo/
__pycache__/
*.pyc

# Claude tooling & session artifacts — keep local, don't push
.claude/
CLAUDE.md
/Resume.md
/Read

# Rendered design diagrams — kept out of the project repo
docs/*diagram*.html

# Tài liệu nội bộ — giữ local, không đẩy lên repo
docs/sprint-*.md
docs/feature-plan-comms-automation-voice.*
docs/ui-redesign-notes.*

# Demo seed — chỉ chạy local, TUYỆT ĐỐI không commit (dữ liệu demo, không thuộc repo)
apps/api/prisma/demo-seed.ts
apps/api/prisma/demo-seed-hust.ts
apps/api/prisma/backfill-password-enc.ts
apps/api/prisma/reset-demo-passwords.ts
