DATABASE_URL=""
JWT_SECRET=""
JWT_EXPIRES_IN=""
PORT=

# Queue (BullMQ) — async notification send + retry. Leave default for local docker redis.
REDIS_URL=redis://localhost:6379

# Notification providers — leave blank for dev (mock mode: logs only, no real send)
SMS_PROVIDER=MOCK          # MOCK | ESMS | TWILIO
SMS_API_KEY=
SMS_SECRET_KEY=
SMS_BRANDNAME=
SMS_FROM=
TEAMS_WEBHOOK_URL=
BROADCAST_API_KEY=
VOICE_PROVIDER=MOCK        # MOCK | STRINGEE | TWILIO
ZALO_PROVIDER=MOCK         # MOCK | ZNS

# Chatbot (tour assistant) — leave default for deterministic mock answers (no LLM).
# OLLAMA = local open-source LLM; GEMINI = hosted fallback (documented, not yet wired).
CHAT_PROVIDER=MOCK         # MOCK | OLLAMA | GEMINI
OLLAMA_URL=                # e.g. http://localhost:11434  (run: ollama serve + ollama pull llama3.1:8b)
OLLAMA_MODEL=llama3.1:8b
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
